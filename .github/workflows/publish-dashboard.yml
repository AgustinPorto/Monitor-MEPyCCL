name: Publish Dashboard

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    # 10:30-17:59 ART (UTC-3) => 13:30-20:59 UTC.
    # Se usa doble grilla (base + offset 2 min) para mitigar retrasos del scheduler de GitHub.
    - cron: "30-59/5 13 * * 1-5"
    - cron: "*/5 14-20 * * 1-5"
    - cron: "32-57/5 13 * * 1-5"
    - cron: "2-57/5 14-20 * * 1-5"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore monitor state
        uses: actions/cache/restore@v4
        with:
          path: |
            .dolar_history.log
            .dolar_monitor_state
          key: monitor-state-${{ github.ref_name }}-${{ github.run_id }}
          restore-keys: |
            monitor-state-${{ github.ref_name }}-

      - name: Install zsh
        run: |
          sudo apt-get update
          sudo apt-get install -y zsh

      - name: Generate dashboard
        run: |
          chmod +x ./monitor.sh
          OUTPUT_HTML="$GITHUB_WORKSPACE/public/dashboard.html" ENV_FILE=/tmp/nonexistent ./monitor.sh || echo "No se pudo actualizar datos; se mantiene dashboard previo."

      - name: Security check
        run: ./security_check.sh

      - name: Save monitor state
        if: always()
        uses: actions/cache/save@v4
        with:
          path: |
            .dolar_history.log
            .dolar_monitor_state
          key: monitor-state-${{ github.ref_name }}-${{ github.run_id }}

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
